## Type

- [ ] Plugin (collaborators)
- [ ] App / docs (maintainers)

## Plugin branch

Must match `plugin_<name>_<version>` (example: `plugin_flip-watch_1.2.0`).

Branch: `plugin_____________________`

`plugin.json` version: `__.__.__`

## What changed

-

## Tested

- [ ] Dropped the folder into `plugins/` (or enabled it in Settings)
- [ ] Price check / vault still works
- [ ] Plugin can be turned off
